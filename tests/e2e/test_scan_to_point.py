"""★ เทสทั้งเส้น: สแกน 1 ใบ → แต้มเข้า loga(ปลอม) → ได้ผลกลับ
เป้าหมาย Step 3 ของ blueprint — พิสูจน์ว่าสถาปัตยกรรมทั้งหมดต่อกันได้

ใช้ skip ไม่ใช่ปล่อยให้แดง เพราะ CI ที่แดงค้างตั้งแต่วันแรกจะสอนให้ทีมเลิกมองสีแดง
พอถึงของจริงที่พัง จะไม่มีใครทันสังเกต
"""
import pytest


@pytest.mark.skip(reason="ยังไม่ถึงคิว — เขียนใน Step 3 (ต่อเส้นทะลุด้วยของปลอม)")
def test_scan_to_point():
    """สแกน → OCR ปลอม → merchant → points → CRM ปลอม → LINE Push ปลอม"""
