# STATE — สถานะปัจจุบันของโปรเจกต์ (อ่านไฟล์นี้ก่อนทำงานต่อ)

> **สำหรับ Claude Code ที่เพิ่งเปิดแชทใหม่:** อ่านไฟล์นี้ก่อน แล้วค่อยอ่าน
> `CONTEXT_for_claude_code.md` → `GETPOINT_blueprint_v3.md` → `docs/decisions/`
>
> ไฟล์นี้บอก "ตอนนี้อยู่ตรงไหน / อะไรเสร็จแล้ว / ทำอะไรต่อ"
> ส่วนสองไฟล์แรกบอก "โปรเจกต์นี้คืออะไร / ตกลงอะไรกันไว้"
>
> **อัปเดตล่าสุด:** หลังจบงานกันใบซ้ำ (ข้อ 3.1 เดิม) — ดู `git log` เพื่อความแม่นยำเสมอ

---

## 1. สรุปโปรเจกต์ใน 30 วินาที

**GETPOINT** = middleware ให้ลูกค้าสแกนใบเสร็จผ่าน LINE → แปลงยอดเงินเป็นแต้ม → ส่งเข้า CRM (loga)

- **V-CLUB** = แบรนด์หน้าบ้านที่ลูกค้าเห็น
- **GETPOINT (เรา)** = LINE OA + LIFF + สแกน + OCR + ตัวกลางเชื่อม loga
- **loga** = CRM สำเร็จรูปที่มีอยู่แล้ว (เราส่ง `cost` + `formula_id` ให้เขาคิดแต้มเอง)

```
ลูกค้าแอด LINE → LIFF → OTP ยืนยันเบอร์ → ถ่ายใบเสร็จ
   → POST /scan ตอบ 202 ทันที → เข้าคิว Redis
   → worker: OpenCV → OCR → แยกยอด → ส่ง loga → LINE Push แจ้งแต้ม
```

---

## 2. ความคืบหน้า (ตาม Step ในพิมพ์เขียว)

| Step | เนื้อหา | สถานะ |
|---|---|---|
| 0 | โครงรัน + settings + db + logging | ✅ เสร็จ |
| 1 | ★ ต่อ loga (ปลด risk) + fake + เทส | ✅ เสร็จ (ยังไม่เคยยิงของจริง — ไม่มี credential) |
| 2 | สมาชิก + OTP + auth + หน้าสมัคร | ✅ เสร็จ (backend + frontend) |
| 3 | ★ ต่อเส้นสแกน→แต้มทะลุทั้งสาย | ✅ เสร็จ (e2e ผ่าน) |
| **4** | **OCR จริง (OpenCV + PaddleOCR)** | ✅ **เสร็จ — ยอดเงินแม่น 96%, ผิด 0%** |
| **5** | merchant + template + Gemini + admin | 🟡 **รู้จักร้านได้แล้ว 96% (ผิด 0) · template/Gemini/admin ยังไม่เริ่ม** |
| **6** | ทนล่ม (retry, circuit breaker, DLQ, Excel) | ✅ **เสร็จ** — กันซ้ำ + retry + breaker + resend + dead letter + Excel + ต่อ worker แล้ว |
| **7** | เก็บงาน (metrics, retention, audit) | 🟡 **retention + metrics + admin + audit(OCR text) + ดูแต้ม/ประวัติ + idempotency เสร็จ** |

**ประมาณ 65-70% ของงานทั้งหมด** · 329 เทสผ่าน (1 skip)

---

## 3. ★ งานถัดไปที่ตกลงกันไว้ (ทำต่อจากตรงนี้)

### 3.1 ✅ กันใบซ้ำ — **เสร็จแล้ว** (ดู ADR 0006)

วัดจริงกับใบเสร็จ 28 รูป: **ใบซ้ำจับได้ 15/15 · หลุด 0 · จับเกิน 0**

สิ่งที่ทำไป:
- ตาราง `receipts` + migration 0002 — ก่อนหน้านี้ระบบ**ไม่เคยจำ**ว่ารับใบไหนไปแล้ว
- `receipt_data/reference_code.py` — ดึงเลขอ้างอิง (เฉพาะที่มีป้ายกำกับ)
- `receipt_check/duplicate_check.py` — ตัดสินด้วยกฎเรียงตามความมั่นใจ 4 ชั้น
- `receipt_identity.py` — ตัด "ชื่อร้าน" ออกจากลายนิ้วมือ (ต้นเหตุของบั๊ก)
- `points/point_rate.py` — 100 บาท = 1 แต้ม (user ระบุ "เอาแบบนี้ไปก่อน")
- `tools/measure_duplicate.py` + `tools/measure_fields.py` — เครื่องมือวัด

### 3.2 สิ่งที่ user ระบุว่าอยากได้ (เรียงตามลำดับ)
1. ✅ ยอดเงินรวม — 27/28 (96%) ตรงเป๊ะ
2. ✅ วันที่ + เวลา — วันที่ 19/28 (68%) · เวลา 21/28 (75%) *(วัดแบบตรงเป๊ะกับเฉลย)*
3. ✅ ชื่อร้าน/สาขา — **รหัสร้าน 27/28 (96%) · ผิด 0**
   เปลี่ยนวิธีวัด: เดิมวัด "ชื่อที่ OCR อ่านได้มีคำของร้านจริงไหม" (64%) ซึ่งไม่มีประโยชน์
   เพราะชื่อนั้นเอาไปตัดสินอะไรไม่ได้ · ตอนนี้วัด **รหัสร้าน** ที่ระบบใช้จริง
   (มาจากเลขผู้เสียภาษีเป็นหลัก — ดู `merchant/known_merchant.py`)
4. 🟡 **ชื่อรายการสินค้า + ราคาต่อรายการ** — อ่านได้แล้ว ยังไม่แม่นพอ
   ```
   ★ ผลรวมราคา = ยอดรวม   12/20 (60%)   ← หลักฐานทางคณิตศาสตร์
   จำนวนรายการตรงเฉลย     15/20 (75%)
   ★ ชื่อสินค้าใกล้เคียง   34/50 (68%)   ← ต่ำเพราะ OCR อ่านคำไทยเพี้ยน
   ```
   วัดด้วย `python -m app.tools.measure_items` (`--show` เพื่อดูทีละรายการ)

> ⚠ ตัวเลขข้อ 2-3 **ต่ำกว่าที่เคยรายงานไว้เดิม** (เดิมเขียนว่า 20/28 กับ 24/28 และ ~15/28)
> เพราะรอบนั้นวัดแค่ "มีค่าไหม" ส่วนรอบนี้วัด "ตรงกับเฉลยไหม" — คนละเกณฑ์

### 3.3 ★ สถานะของ "อ่านรายการสินค้า" (`receipt_data/line_items.py`)

**ทำไปแล้ว:** อ่านรายการ + ราคาได้ · ตัวช่วย `items_match_total()` พร้อมให้
`template_rules.py` ใช้ตอน Step 5 (CONTEXT ข้อ 4 เรียกกฎนี้ว่า "ตัวทรงพลังสุด")

**ยังไม่ได้ทำ (ตั้งใจ):**
- ❌ **ไม่เอา items ไปตัดสินใบซ้ำ** — อันตราย เพราะรูปคนละมุมของ*ใบเดียวกัน*
  อ่านรายการได้ไม่เท่ากัน (ใบ KFC #1 ได้ 6 รายการ · #28 ได้ 3)
  ถ้าใช้กฎ "รายการต่างกัน = คนละใบ" ใบซ้ำจะหลุด = ให้แต้มสองเท่า
  → รอให้ความแม่นสูงกว่านี้ก่อน · ตอนนี้ "เวลา" ทำหน้าที่นี้อยู่และได้ 15/15
- ❌ **ไม่เก็บ items ลงตาราง** — ยังไม่มีใครใช้ (เพิ่มคอลัมน์เมื่อมีคนใช้จริง)
- ❌ **ยังไม่ใช้พจนานุกรมไทย** — ดูข้อ 10.1

**ข้อจำกัดที่วัดได้:** ใบที่ OCR จับคู่คอลัมน์เหลื่อมแถว (ชื่ออยู่แถวหนึ่ง ราคาอีกแถว)
อ่านราคาผิดคู่ — เจอกับสลิปศูนย์อาหาร BigC (#11/#14/#16/#19) ที่ถ่ายเอียง
→ **แก้ตรงจุดด้วย template ต่อร้าน (Step 5)** ซึ่งรู้ว่าคอลัมน์ราคาอยู่พิกัดไหน

### 3.4 ★ Step 5 — ทำ "รู้จักร้าน" เสร็จแล้ว (ที่เหลือยังไม่เริ่ม)

**เสร็จ:** `merchant/known_merchant.py` + `merchant/merchant_resolver.py`
```
รหัสร้าน  27/28 (96%) · ★ ผิด 0/28
```
สัญญาณหลักคือ **เลขผู้เสียภาษี 13 หลัก** ไม่ใช่ชื่อร้าน (ชื่อร้านอ่านไม่คงที่)
เพิ่มร้านใหม่ = เอาใบเสร็จจริงมาดูเลขภาษี แล้วเติมแถวใน `KNOWN_MERCHANTS`

ผลพลอยได้ 2 อย่าง:
- ลูกค้าเห็นชื่อร้านที่ถูกต้อง ("KFC") แทนข้อความมั่วจาก OCR
- duplicate_check มีกฎใหม่: **คนละร้านที่รู้จักทั้งคู่ = คนละใบแน่นอน**
  (กันเคสซื้อของราคาเท่ากันสองร้านในห้างเดียวกัน แล้วใบที่สองถูกบล็อก)

**เสร็จเพิ่ม:** `template_rules.py` — กฎตรวจค่าทางคณิตศาสตร์ (CONTEXT ข้อ 4)
```
math ยืนยัน (ผลรวมรายการ=ยอดรวม)  13/27 ใบ
hard failure (ยอดนอกช่วง/วันอนาคต) 0/27  ← ไม่มี false positive
```
ต่อเข้า field_extractor แล้ว · วันนี้เป็น "สัญญาณความมั่นใจ" ที่ log ไว้เท่านั้น
ยังไม่เอาไปกรอง (ยอดแม่น 96% อยู่แล้ว) · จะใช้จริงตอนเลื่อนขั้น template

**เสร็จเพิ่ม:** prompt_guard + Gemini (ตัวสำรองอ่านใบเสร็จตอนกฎอ่านไม่ได้)
- `security/prompt_guard.py` — ล้าง OCR text ก่อนเข้า AI (กัน prompt injection)
- `external/ai_interface.py` + `gemini_client.py` + `fake_ai.py` — adapter + port + fake
- `merchant/gemini_resolver.py` — ต่อ 3 ชั้น: sanitize → AI เดา → template_rules ตรวจ
  ★ ห้ามเชื่อ AI โดยไม่ผ่านกฎ (ต่อให้ injection หลุด กฎคณิตศาสตร์จับค่ามั่วได้)

⚠ **ยังไม่ต่อเข้า scan_job** — ต้องตั้ง `GEMINI_API_KEY` + `pip install google-genai` ก่อน
  โครงพร้อม เทสครบ (gemini_client ใช้ lazy import ไม่พังถ้ายังไม่ติดตั้ง) รอเปิดสวิตช์ที่ composition

**ยังไม่เริ่ม (ตั้งใจ — ของหนักที่ blueprint เลื่อนไว้):**
template_matcher (ต้องใช้ bounding box) · template lifecycle · หน้า admin ·
ตาราง merchants/templates

**หมายเหตุ:** วันที่ 68% / เวลา 75% ที่ยังต่ำ ส่วนใหญ่เป็น date=None (อ่านไม่เจอ)
กับ OCR อ่านเลขผิด (#13 06→05, #14 17→12) → แก้ได้ด้วย per-shop coordinate template
ที่รู้พิกัดบรรทัดวันที่ ซึ่งต้องมี bounding box (ตอนนี้ flatten เป็น text แล้ว) — งานใหญ่

---

## 4. ★ ของจริง vs ของชั่วคราว (สำคัญมาก — อย่าเข้าใจผิด)

user ตกลงไว้ว่า **"สร้างของจริงต่อไป ส่วนที่ชั่วคราวเก็บไว้ แล้วค่อยถอดทีเดียวตอนพร้อม deploy จริง"**

### ของจริง (production code)
| ส่วน | สถานะ |
|---|---|
| loga client (ส่งแต้ม/หาสมาชิก/สมัคร) | ยิง HTTP ไป loga จริง |
| LINE token verify | ยิงไป `api.line.me` จริง |
| OTP (สุ่ม/hash/Redis/หมดอายุ/ล็อก) | ของจริง |
| OCR (PaddleOCR + OpenCV) | ของจริง รันในเครื่อง |
| DB + migration | ของจริง (Postgres + Alembic) |

### ของชั่วคราว (ต้องถอด/ต่อของจริงก่อนใช้งานจริง)
| ส่วน | ตอนนี้ | ต้องทำอะไร |
|---|---|---|
| **ส่ง SMS** | `FakeSms` (OTP โผล่ใน log) | เลือก vendor + เขียน `external/sms_client.py` ให้เสร็จ + ตั้ง `SMS_API_KEY` |
| **LINE Push** | `FakeNotifier` ถ้าไม่มี token | ตั้ง `LINE_CHANNEL_TOKEN` (Messaging API) |
| **frontend preview mode** | จำลอง LINE + API (OTP = `123456`) | ตั้ง `VITE_LIFF_ID` แล้วมันใช้ของจริงอัตโนมัติ |
| **ดีไซน์หน้าจอ** | แบบ 4 (ม่วง-เหลือง) เป็น placeholder | user จะออกแบบใหม่ (อาจใช้ Figma) — แก้แค่ `frontend/src/styles/tokens.css` |
| **loga credential** | ยังไม่มี | ใส่ `LOGA_*` ใน `.env` แล้วรัน `pytest -m integration` |

**การสลับของจริง/ปลอมทั้งหมดอยู่ที่ `backend/app/composition.py` ที่เดียว** (ตัดสินจาก config)

---

## 5. สถานะ OCR (งานหลักที่เพิ่งทำเสร็จ)

### ผลวัดจริงกับใบเสร็จจริง 28 ใบ
```
ยอดเงิน:  ถูก 27/28 (96%) · ★ ผิด 0/28 (0%) · อ่านไม่ได้ 1/28
วันที่:   มีค่า 20/28
เวลา:     มีค่า 24/28
ชื่อร้าน: อ่านเป็นชื่อร้านจริง ~15/28
ความเร็ว: 7-9 วินาที/ใบ  (SLO < 15 วินาที ✓)
```

### วิธีวัดซ้ำ (ทำทุกครั้งที่แก้โค้ด OCR หรือตัวแยก field)
```bash
cd backend
python -m app.tools.measure_ocr        # ยอดเงินอย่างเดียว (exit 1 ถ้ามีใบอ่านผิด)
python -m app.tools.measure_fields     # ★ ทุก field เทียบเฉลย
python -m app.tools.measure_duplicate  # ★ กันใบซ้ำ (exit 1 ถ้ามีใบซ้ำหลุด)
python -m app.tools.measure_items      # ★ รายการสินค้า (--show ดูทีละรายการ)
python -m app.tools.try_ocr <ไฟล์|โฟลเดอร์>   # ดูว่าอ่านอะไรได้บ้าง
```

⚠ `measure_fields` / `measure_duplicate` **แคชผล OCR ไว้** (`.ocr_cache.json`)
แคชเก็บแค่ "บรรทัดดิบจาก OCR" ส่วนการแยก field คำนวณใหม่ทุกครั้ง
→ แก้ตัวแยก field รันได้เลย · **แก้ `image_prep`/`paddle_ocr` ต้องสั่ง `--fresh`**

เฉลยอยู่ที่ `tests/fixtures/expected/receipts_ground_truth.json`
(อ่านจากรูปด้วยตา ครบ 28 ใบ — ยอด/วันที่/เวลา/ร้าน/เลขอ้างอิง/รายการสินค้า/กลุ่มใบซ้ำ)
คำอธิบายเฉลย + เคสกับดัก อยู่ที่ `tests/fixtures/expected/README.md`

### ★ ชุดทดสอบไม่ใช่ใบเสร็จ 28 ใบ แต่เป็น...
**การซื้อ 12 ครั้ง · กระดาษ 14 ใบ · ถ่ายใบละ 2 มุม = 28 รูป**
(user ตั้งใจให้ตั้งแต่แรกว่าจะทดสอบ "ใบเดิมหลายมุม")
→ ใช้เป็นเฉลยของการกันใบซ้ำได้ทันทีโดยไม่ต้องหารูปเพิ่ม

### ★ หลักที่ยึดตลอด (อย่าเปลี่ยนโดยไม่ถาม)
> **ให้แต้มผิด เสียหายกว่า ไม่ให้แต้ม**
> เมื่อไม่มั่นใจ ระบบต้องบอก "อ่านไม่ได้ ถ่ายใหม่" ไม่ใช่เดาตัวเลข
>
> เคยยอมให้ครอบคลุมลดจาก 72% → 66% เพื่อกำจัดคำตอบที่ผิด — และมันถูกต้อง

### สถาปัตยกรรม OCR (ชั้นที่เราเขียนเองทับ PaddleOCR)
```
image_prep/     ตัดขอบ → ดัดเอียง (projection profile) → CLAHE → ตรวจคุณภาพ
ocr/            PaddleOCR (th_PP-OCRv5) + รวมกล่องข้อความกลับเป็นบรรทัด
receipt_data/
  amount_parser.py   รู้ว่า "อะไรไม่ใช่เงิน" (เวลา/วันที่/รหัส/เลขติดตัวอักษร)
  total_finder.py    ★ ยืนยันยอดด้วยหลายชั้น (คำสำคัญ + คณิตศาสตร์ + กฎสลิปบัตร)
  datetime_parser.py วันที่ + เวลา (รองรับ พ.ศ., ชื่อเดือน, ทวิภาคหาย, PM อ่านเพี้ยน)
  merchant_name.py   ชื่อร้าน (ให้คะแนนบรรทัดส่วนหัว, ตัดชื่อธนาคารออก)
  reference_code.py  ★ เลขอ้างอิงธุรกรรม (เฉพาะที่มีป้ายกำกับ — ดู ADR 0006)
  line_items.py      ★ รายการสินค้า + ราคา (ไล่ขึ้นจากบรรทัดยอดรวม)
  receipt_identity.py ลายนิ้วมือรูป + ลายนิ้วมือเนื้อหา
  field_extractor.py ตัวประกอบ
receipt_check/
  duplicate_check.py ★ ตัดสินว่าใบนี้เคยได้แต้มไปแล้วหรือยัง (กฎ 4 ชั้น)
```

**สิ่งที่ทำให้แม่นขึ้นจาก 3% → 96% คือชั้นนี้ทั้งหมด ไม่ได้แตะ PaddleOCR เลย**

---

## 6. คำสั่งที่ใช้บ่อย

```bash
# เทส (จาก repo root)
pytest                      # ทั้งหมด (280 ผ่าน, 1 skip)
pytest -m integration       # ยิง loga จริง (skip อัตโนมัติถ้าไม่มี credential)

# วัด OCR (จาก backend/)
python -m app.tools.measure_ocr
python -m app.tools.try_ocr ../tests/fixtures/receipts

# รันระบบ
docker-compose up                                    # ทั้งชุด
uvicorn app.main:app --host 0.0.0.0 --port 8000      # web (จาก backend/)
python -m app.worker                                 # worker (จาก backend/)
npm run dev --prefix frontend                        # frontend (localhost:5173)

# migration (จาก backend/)
alembic upgrade head
```

---

## 7. สภาพแวดล้อม + กับดักที่เคยเจอ (อย่าเสียเวลาซ้ำ)

- **Windows + Python 3.11** · terminal ต้องตั้ง `$env:PYTHONUTF8=1` ไม่งั้นภาษาไทยเพี้ยน
- **ไฟล์ `.ini` ต้องเป็น ASCII เท่านั้น** — `fileConfig()` อ่านด้วย cp874 ทำให้ Alembic พัง
  (คอมเมนต์ไทยใช้ได้ใน `.py` เพราะ Python อ่าน UTF-8 เสมอ)
- **ปักหมุดเวอร์ชัน** `paddleocr==3.3.0` ไว้แล้ว — PaddleOCR เปลี่ยน API ระหว่างเวอร์ชันใหญ่
- **โมเดล OCR อยู่ในเครื่อง** `C:\Users\ASUS\.paddlex\official_models\` (136 MB) ไม่ต่อเน็ต
- **git**: repo อยู่ที่ `getpoint/.git` เอง · ⚠ โฟลเดอร์แม่ `Desktop/` มี `.git` อีกอันที่ผูกกับ
  โปรเจกต์อื่น (line bot food) — ยืนยัน `git rev-parse --show-toplevel` ก่อนเสมอ
- **GitHub**: user ตัดสินใจ **ยังไม่ push จนกว่าจะเสร็จ** (commit ในเครื่องครบทุกก้าว)

---

## 8. สไตล์การทำงานที่ user ต้องการ (สำคัญ — เพิ่มเติมจาก CONTEXT ข้อ 7)

> 💡 **มี skill `/DEV` ติดตั้งไว้แล้ว** (`~/.claude/skills/DEV/SKILL.md`)
> รวมมาตรฐานทั้งหมดนี้ในรูปแบบที่ใช้ได้กับทุกโปรเจกต์ — เรียก `/DEV` เมื่อเริ่มงานได้เลย

- **เขียนทีละไฟล์ + อธิบายว่าทำอะไร ทำไมเขียนแบบนี้** ทุกครั้ง
- **คอมเมนต์เป็นภาษาไทย** · ชื่อไฟล์/ตัวแปรเป็นอังกฤษ
- **1 ไฟล์ = 1 หน้าที่** ชื่อไฟล์อ่านแล้วรู้ทันทีว่าทำอะไร
- **วัดผลจริงเสมอ อย่าเดา** — user ให้ค่ากับตัวเลขที่พิสูจน์ได้มากกว่าคำอธิบาย
- **บอกตรงๆ เมื่อมีปัญหา** อย่าเออออ · เตือนเมื่อเห็นอะไร over-engineer
- **ถามก่อนถ้าจะเปลี่ยนสิ่งที่ตกลงไว้** (CONTEXT ข้อ 2-3)
- user เป็น BA/PM มีพื้นเทคนิค — อยากเข้าใจทุกอย่างที่เขียน ไม่ใช่แค่ได้โค้ด
- **commit บ่อยๆ พร้อมข้อความอธิบายละเอียด** (ดู `git log` เป็นตัวอย่าง)

---

## 9. หนี้ที่ค้างอยู่ (รู้แล้วแต่ยังไม่แก้ — มีเหตุผล)

| หนี้ | เหตุผลที่ยังไม่แก้ | จะแก้เมื่อไหร่ |
|---|---|---|
| `find_customer` กลืน error ทุกชนิดเป็น "ไม่พบสมาชิก" | ไม่รู้ code จริงของ loga | เมื่อได้ credential |
| `AUTH_ERROR_CODES = {401, 403}` เป็นการเดา | เอกสาร loga ไม่ระบุ | เมื่อได้ credential |
| สมาชิกที่เราสมัครเป็น "บัตรพลาสติก" (pcard_id) ไม่ใช่ uid | ต้องตัดสินใจเชิงธุรกิจ | ก่อน Step 2 ใช้งานจริง (ดู ADR 0003) |
| ใบเสร็จ #17 อ่านไม่ได้ | OCR อ่านเลขผิดตั้งแต่ต้นทาง (76 แทน 75) | ต้องใช้ template ของร้าน (Step 5) |
| `SmsClient` ยัง `NotImplementedError` | ยังไม่เลือก vendor (user บอกรอก่อน ไม่อยากเสียเงิน) | ก่อนเปิดใช้จริง |
| คอลัมน์ในไฟล์ Excel export เดาจากพารามิเตอร์ add_customer_point | ยังไม่เห็นหน้า Import จริงของ loga | เมื่อได้ credential — ปรับ `_HEADERS` ใน excel_export.py ให้ตรง |
| หน้า admin เป็น API อย่างเดียว (ยังไม่มี UI) | ทีมดูแลเรียกผ่าน curl/tool ได้ | ทำ UI เมื่อมีคนใช้จริง (ไม่เร่ง) |
| template lifecycle เต็ม (candidate→official + หน้าอนุมัติ) ยังไม่ทำ | ของหนักที่ blueprint เลื่อน + ต้องใช้ bounding box | ทำเมื่อมีร้านที่กฎ+เลขภาษีเอาไม่อยู่จริง |
| ★ แต้มที่แจ้งลูกค้า (100 บาท = 1 แต้ม) เป็นค่าที่ **เราคำนวณเอง** | loga คิดแต้มจาก `formula_id` ฝั่งเขา และไม่คืนค่า "รายการนี้ได้กี่แต้ม" กลับมา | ต้องตั้งสูตรฝั่ง loga ให้เป็น 100:1 แล้วยิงจริงยืนยัน 1 ครั้ง — ไม่งั้นเลขที่แจ้งลูกค้าไม่ตรงกับแต้มจริง |
| อ่านเลขอ้างอิงได้แค่ 7/14 เอกสาร | ป้ายกำกับบางใบ OCR อ่านเพี้ยนจนจับไม่ได้ | ดีขึ้นเองเมื่อมี template ต่อร้าน (Step 5) |

---

## 10. คำถามที่ user ถามค้างไว้ (ยังไม่ได้ตอบ/ยังไม่ได้ทำ)

### 10.1 ★ พจนานุกรมภาษาไทย — ตกลงกันแล้วว่า "จำเป็นตอนทำรายการสินค้า"

user เสนอให้โหลดคลังคำภาษาไทยเก็บไว้ช่วย OCR · ตอนแรกผมประเมินว่าไม่คุ้ม
แต่ **user ชี้ถูกว่าผมมองแคบไป** — ข้อสรุปที่ตกลงกัน:

| ใช้กับอะไร | ต้องมีพจนานุกรมไหม |
|---|---|
| **คำสำคัญ** (รวมทั้งสิ้น/ยอดสุทธิ/เงินทอน) | ❌ ไม่ต้อง — มีไม่กี่สิบคำ ใช้ fuzzy เทียบกับรายการที่เรารู้อยู่แล้วได้ (ทำไปแล้ว ได้ผลจริง) |
| **ชื่อร้าน** | ⚠ ช่วยได้บ้าง — แต่ template ต่อร้าน (Step 5) แก้ตรงจุดกว่า |
| **★ ชื่อสินค้า** (งานข้อ 4 ที่ user อยากได้) | ✅ **จำเป็น** |

**เหตุผลที่ชื่อสินค้าต่างจากคำอื่น:** ชื่อสินค้าเป็นเซตเปิด ไม่มีวันครบและไม่ซ้ำเดิม
(`น้ำพริกหนุ่มตราแม่ศรี`, `ชาไทยเย็นหวานน้อย`) → ไม่มีรายการให้เทียบแบบคำสำคัญ
พจนานุกรมจะช่วยตัดสินว่า `ซี่โดรงหมูอบ` ที่ OCR อ่านมา ควรเป็น `ซี่โครงหมูอบ`

**แนวทางเมื่อถึงตอนนั้น (Step หลังจาก 5):**
- ใช้คลังคำไทยแบบ offline (เช่น รายการคำจาก PyThaiNLP) — ห้ามเรียก API ภายนอก
- เก็บ "คลังคำเฉพาะใบเสร็จ" ของเราเองเพิ่ม — คำที่เจอจริง + รูปที่ OCR อ่านเพี้ยน
  โตขึ้นเองทุกครั้งที่เจอใบใหม่ (ตรงกับที่ user บอก "เจอคำไม่คุ้นก็ได้เรียนรู้")

**★ วัดแล้วว่าจำเป็นแค่ไหน (ข้อมูลใหม่ หลังทำ `line_items.py` เสร็จ):**
ชื่อสินค้าอ่านใกล้เคียงเฉลย 34/50 (68%) — ที่พลาดส่วนใหญ่เป็นคำไทยที่อ่านเพี้ยนหนัก
```
"กระดาษการ์ดขาว A4 200G"  → OCR อ่านได้ "กระตายตาะแA4 200L035"
"ข้าวเปล่า"                → OCR อ่านได้ "ซ้จวคาก"
```
เคสแรกพจนานุกรมช่วยได้ (เพี้ยน 2-3 ตัว) · **เคสหลังช่วยไม่ได้** (เพี้ยนจนไม่เหลือเค้า)
→ ตัวที่แก้เคสหลังคือ **คุณภาพรูป/template ต่อร้าน** ไม่ใช่พจนานุกรม
→ ข้อเสนอ: ทำ template (Step 5) ก่อน แล้ววัดใหม่ ค่อยตัดสินใจเรื่องพจนานุกรม

### 10.2 ✅ ใบเสร็จ + สลิปบัตรของรายการเดียวกัน ควรได้แต้มกี่ครั้ง — **ตอบแล้ว**

user ตอบว่า **"แต้มคิดได้ครั้งเดียว"** → ระบบทำตามนี้แล้ว (จับได้ทั้ง 2 คู่ในชุดทดสอบ)

### 10.3 ✅ อัตราแต้ม — **ตอบแล้ว**

**100 บาท = 1 แต้ม** (user: "เอาแบบนี้ไปก่อน ค่อยมาปรับเปลี่ยน")
อยู่ที่ `points/point_rate.py` ที่เดียว · ปัดลงเสมอ
⚠ ดูหนี้ข้อสุดท้ายในตารางข้อ 9 — ยังต้องตั้งสูตรฝั่ง loga ให้ตรงกัน
