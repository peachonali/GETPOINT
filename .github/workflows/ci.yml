name: ci
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: setup python + node
      # TODO: generate TS types จาก receipt_schema.py
      # TODO: run pytest + vitest
